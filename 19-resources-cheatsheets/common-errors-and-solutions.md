# Common Errors & Solutions

- `Permission denied (publickey)` → add SSH key to GitHub.
- `fatal: couldn't find remote ref` → check branch name.
- Actions `workflow is not valid` → YAML indentation issue.
